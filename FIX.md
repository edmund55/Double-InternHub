Supervisor -> Answer: The description and image upload still appear even after Sent the answer, fix it.
Separate mockData of rawOnboardingStep for each modules in different files. Instruction step data type should be optional of text, image, link, code snippet. Fix the data structure.
Toast effect when sign in and sign up, and any other action that involve CRUD operation (make the toast appear from top center, give a better styling to the toast)
Onboarding: Mark done button can be unmark (change the style between marked and not mark)
Intern -> Ask -> Question history: can also be filter using category and status
Announcements: render latest one at top for both intern and supervisor
Change email for every user to this format: yourname@internal.double.my
